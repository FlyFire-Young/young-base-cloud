debug: false
logging:
  level: info
server:
  port: 8760
eureka:
  instance:
      hostname: localhost
  client:
      serviceUrl:
          defaultZone: http://${eureka.instance.hostname}:8761/eureka/
spring:
  application:
    name: spring-cloud-eureka